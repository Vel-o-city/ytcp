import type { McpServer } from "@modelcontextprotocol/sdk/server/mcp.js";

export function registerTools(_server: McpServer): void {
  // Phase 1 wires the registration surface; tools arrive in later plans/phases.
}
